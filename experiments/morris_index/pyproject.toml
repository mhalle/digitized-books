[project]
name = "morris-index"
version = "0"
description = "Build a SQLite index of Morris Anatomy 1914 from Wellcome IIIF + ALTO"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "lxml>=4.9",
    "sqlite-utils>=3.35",
]

[tool.uv]
managed = true
